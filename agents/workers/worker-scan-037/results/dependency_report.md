# Dependency Report: commit-relay

**Generated**: 2025-11-23T18:49:00Z
**Worker**: worker-scan-037

---

## Summary

| Project | Dependencies | Vulnerable | Status |
|---------|--------------|------------|--------|
| Root (commit-relay) | 316 | 0 | Clean |
| api-server | 109 | 0 | Clean |
| eui-dashboard | 599 | 9 | Needs Attention |
| llm-mesh/middleware | Unknown | Unknown | No Lockfile |

---

## Project Details

### Root Project (commit-relay)

**Status**: Clean
**Dependencies**: 316 (8 prod, 305 dev, 5 optional)

No vulnerabilities found. All dependencies are up to date with security patches.

---

### api-server

**Status**: Clean
**Dependencies**: 109 (93 prod, 15 dev, 1 optional)

No vulnerabilities found. Core API server dependencies are secure.

---

### eui-dashboard

**Status**: Needs Attention
**Dependencies**: 599 (317 prod, 283 dev, 95 optional)

#### Vulnerable Packages

| Package | Current Version | Fixed Version | Severity | Notes |
|---------|----------------|---------------|----------|-------|
| d3-color | <3.1.0 | 3.1.0+ | HIGH | ReDoS vulnerability |
| d3-interpolate | 0.1.3-2.0.1 | 2.0.2+ | HIGH | Via d3-color |
| esbuild | <=0.24.2 | 0.25.0+ | MODERATE | Dev server issue |
| prismjs | <1.30.0 | 1.30.0+ | MODERATE | DOM Clobbering |
| refractor | <=4.6.0 | 4.7.0+ | MODERATE | Via prismjs |
| vite | 0.11.0-6.1.6 | 6.1.7+ | MODERATE | Via esbuild |
| vite-node | <=2.2.0-beta.2 | 2.2.0+ | MODERATE | Via vite |
| vitest | Various | 4.0.13+ | MODERATE | Multiple issues |
| @elastic/eui | >=33.0.0 | 32.3.0 | MODERATE | Via refractor |

#### Fix Strategy

Most issues can be resolved by updating vitest:

```bash
cd eui-dashboard
npm install vitest@^4.0.13
```

**Note**: This is a major version update from vitest v2 to v4. Test thoroughly.

For @elastic/eui issues, the fix requires a downgrade which may break features:
- Consider if prismjs DOM Clobbering affects your use case
- The vulnerability requires specific conditions to exploit

---

### llm-mesh/gateway/middleware

**Status**: Not Audited
**Reason**: No package-lock.json file

To enable auditing:
```bash
cd llm-mesh/gateway/middleware
npm install --package-lock-only
npm audit
```

---

## License Analysis

Based on the scan policy, the following licenses are:

**Allowed**: MIT, Apache-2.0, BSD-3-Clause, ISC

**Blocked**: GPL-3.0, AGPL-3.0

No blocked licenses were detected in the audited packages.

---

## Recommendations

### Quick Fixes

1. **vitest update** (fixes 6 vulnerabilities):
   ```bash
   cd eui-dashboard
   npm install vitest@^4.0.13
   npm test  # Verify tests still pass
   ```

2. **Generate middleware lockfile**:
   ```bash
   cd llm-mesh/gateway/middleware
   npm install --package-lock-only
   ```

### Longer-term Improvements

1. **Enable automated updates**:
   - Add Dependabot configuration
   - Or use Renovate for more control

2. **Pre-commit hooks**:
   - Add `npm audit` to pre-commit checks
   - Block commits with critical vulnerabilities

3. **CI/CD Integration**:
   - Run `npm audit --audit-level=high` in pipeline
   - Fail builds on high/critical findings

---

## Version Recommendations

Consider updating to these versions for security:

| Package | Recommended Version |
|---------|-------------------|
| vitest | 4.0.13+ |
| vite | 6.1.7+ (if direct dep) |
| prismjs | 1.30.0+ |
| d3-color | 3.1.0+ |

---

*Report generated by commit-relay security scan worker*
